# Build the project

First build:

```bash
cmake -B build
cmake --build build
```

Run the program:

```bash
./build/OS2_AS2
```

## Rebuild after changes

After modifying the code, rebuild using:

```bash
cmake --build build
```

Then run again:

```bash
./build/OS2_AS2
```